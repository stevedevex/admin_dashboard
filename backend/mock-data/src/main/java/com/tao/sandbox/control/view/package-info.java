/**
 * The JSON the dashboard receives, as records.
 *
 * <p>Separate from the domain types on purpose. These are a published contract with a client that
 * ships separately, so a field is removed here only deliberately — whereas the types below are free
 * to change shape whenever the reasoning does.
 */
package com.tao.sandbox.control.view;
